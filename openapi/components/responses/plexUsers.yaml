description: A JSON array containing user information
content:
  application/json:
    type: array
    schema:
      oneOf:
       - $ref: ../schemas/plexUser.yaml
       - $ref: ../schemas/tautulliUser.yaml
    example:
      $ref: ../examples/plexUsers.yaml
