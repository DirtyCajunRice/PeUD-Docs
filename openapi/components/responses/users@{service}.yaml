description: A JSON array containing user information
content:
  application/json:
    schema:
      oneOf:
       - type: array
         items:
           $ref: ../schemas/plex/user.yaml
       - type: array
         items:
           $ref: ../schemas/tautulli/user.yaml
       - type: array
         items:
           $ref: ../schemas/organizr/user.yaml
