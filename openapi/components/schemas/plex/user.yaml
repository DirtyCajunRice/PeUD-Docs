type: object
properties:
  id:
    $ref: ID.yaml
  username:
    $ref: username.yaml
  email:
    $ref: email.yaml
  thumb:
    $ref: thumb.yaml
  title:
    $ref: title.yaml
  home:
    $ref: home.yaml
  restricted:
    $ref: restricted.yaml
  protected:
    $ref: protected.yaml
  filterAll:
    $ref: filterAll.yaml
  filterMovies:
    $ref: filterMovies.yaml
  filterMusic:
    $ref: filterMusic.yaml
  filterPhotos:
    $ref: filterPhotos.yaml
  filterTelevision:
    $ref: filterTelevision.yaml
  allowTuners:
    $ref: allowTuners.yaml
  allowSync:
    $ref: allowSync.yaml
  allowCameraUpload:
    $ref: allowCameraUpload.yaml
  allowChannels:
    $ref: allowChannels.yaml
  recommendationsPlaylistId:
    $ref: recommendationsPlaylistId.yaml
  plexUserServers:
    type: array
    items:
      $ref: server/userServer.yaml

